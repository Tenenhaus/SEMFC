% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_correlation_coeff.R
\name{get_correlation_coeff}
\alias{get_correlation_coeff}
\title{Construct Correlation Matrix from Parameter Vector}
\usage{
get_correlation_coeff(x, latent_variables, start_index)
}
\arguments{
\item{x}{A numeric vector of parameters. Correlation-related values are extracted starting
from the index specified by \code{start_index}.}

\item{latent_variables}{A vector of names or identifiers for the latent variables, used
to determine the dimensions of the correlation matrix.}

\item{start_index}{An integer specifying the index in \code{x} where the correlation parameters
begin.}
}
\value{
A symmetric correlation matrix (dim x dim), where \code{dim} is the number of latent variables.
}
\description{
This function constructs a symmetric correlation matrix for a set of latent variables
based on the parameter vector \code{x} and the starting index for correlation-related parameters.
}
\examples{
\dontrun{
latent_variables <- c("LV1", "LV2", "LV3", "LV4")
x <- c(0.5, 0.3, 0.2, 0.5, 0.4, 0.2) # upper triangle values for a 4x4 matrix
start_index <- 1
get_correlation_coeff(x, latent_variables, start_index)}
}
\keyword{internal}
