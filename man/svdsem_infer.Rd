% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_svd.R
\name{svdsem_infer}
\alias{svdsem_infer}
\title{Statistical Inference and Goodness-of-Fit for SVD-Estimated Models}
\usage{
svdsem_infer(fit, B, verbose = TRUE, seed = NULL)
}
\arguments{
\item{fit}{A svdSEM fit object containing the fitted model information, including
parameter estimates and model-implied covariance matrix.}

\item{B}{Integer specifying the number of bootstrap replications to perform.}

\item{verbose}{Logical indicating whether to display a progress bar during
bootstrap resampling (default: \code{TRUE}).}

\item{seed}{Optional integer for reproducibility of bootstrap results. If \code{NULL}, no seed is set (default: \code{NULL}).}
}
\value{
A list containing two main components:
\itemize{
\item \code{result}: List with bootstrap and inference results:
\itemize{
\item \code{boot}: List of bootstrap replications for all parameters (see \code{bootstrap_svd()}).
\item \code{infer}: Formatted tables with parameter estimates, standard errors, z-statistics,
and p-values (see \code{formatting_svd_infer()}).
}
\item \code{gof}: List with goodness-of-fit test results:
\itemize{
\item \code{T_LS}: Observed test statistic from the fitted model.
\item \code{Tb_LS}: Vector of bootstrap test statistics.
\item \code{pval}: Bootstrap p-value for the goodness-of-fit test.
\item \code{improper}: Number of improper bootstrap solutions (negative eigenvalues).
}
}
}
\description{
Performs bootstrap-based statistical inference and computes goodness-of-fit statistics
for models estimated using the SVD method. Combines bootstrap replications with
Bollen-Stine bootstrap test results.
}
\keyword{internal}
